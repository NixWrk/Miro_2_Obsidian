# Short Label Visual Vertical Clearance

This fixture protects the rule from `CONV-010`: short labels such as `RICE` and `Кано` can sit just above image previews in Miro, while Obsidian's text node box overlaps the image preview after auto-height.

The converter should move the short label far enough above the visual node to keep a stable clearance without changing long text blocks.
