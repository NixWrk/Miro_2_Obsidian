Protects the `obsidian` text style mode.

Simple Miro HTML should become native Obsidian Markdown/plain text without the
outer `font-size` wrapper. Rich visual inline HTML, such as highlighted spans,
must remain HTML, but the converter should avoid adding an extra Miro-style
font-size wrapper around it.
