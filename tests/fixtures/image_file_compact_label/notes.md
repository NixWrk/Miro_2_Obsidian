Regression fixture for `CONV-008`.

Obsidian displays a title/chrome for file preview nodes. Long exported image
filenames such as `sample_board_export_<id>.png` visually dominate nearby
text labels, so converted image references use compact stable names.
